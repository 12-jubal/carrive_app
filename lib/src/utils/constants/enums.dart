// Define all the enums here

// User type enum
enum UserType { driver, passenger }

// Toast type enum
enum ToastType { success, error, warning, info }

// validation Text field type enum
enum ValidationType { valid, invalid }

// Dio Methods
enum DioMethod { post, get, put, delete }

// Bottom Nav bar items
enum NavbarItem { explore, chat, bookings, profile }
